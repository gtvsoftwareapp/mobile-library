<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.modelproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ModelProject">
        <activity android:name=".ui.act001.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.act003.MenuActivity" />
        <activity android:name=".ui.act002.ConfiguracaoActivity" />
        <activity android:name=".ui.act002.MinhaContaActivity" />
        <activity android:name=".ui.act000.LeituraActivity" />

        <activity android:name=".ui.act004.FiltroUGBActivity" />

        <activity android:name=".ui.act000.ConsultaColaboradorActivity" />
        <activity android:name=".ui.act000.ConsultaFilialActivity" />
        <activity android:name=".ui.act000.ConsultaUGBActivity" />

    </application>

</manifest>